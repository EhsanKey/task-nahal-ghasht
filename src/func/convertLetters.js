const convertLetters = (number) => {
  console.log(number);
  if (number === 3) {
    return "سه";
  } else if (number === 4) {
    return "چهار";
  } else {
    return "پنج";
  }
};

export default convertLetters;
